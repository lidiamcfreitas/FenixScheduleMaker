# -*- mode: python -*-
a = Analysis(['schedule2.py'],
             pathex=['/Users/Armstrong/Git/FenixScheduleMaker/ScheduleMaker'],
             hiddenimports=[],
             hookspath=None,
             runtime_hooks=None)
pyz = PYZ(a.pure)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='schedule2',
          debug=False,
          strip=None,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=None,
               upx=True,
               name='schedule2')
